- defaults:
    link:
        relink: true
        create: true

- clean: ['~']

- link:
    ~/.bash_profile:
        path: bash_profile
        force: false
    ~/.bashrc:
        path: bashrc
        force: false

- shell:
    - [git submodule update --init --recursive, Installing submodules]
