return {
  {
    "neovim/nvim-lspconfig",
    opts = {
      inlay_hints = {
        -- Hints make the code hard to read. It can
        -- always be toggled on if needed.
        enabled = false,
      },
    },
  },
}
